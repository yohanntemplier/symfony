{% extends 'base.html.twig' %}

{% block title %}Tag {{ tag.name }}{% endblock %}

{% block body %}
    <h1 class="text-center m-3">{{ tag.name }}</h1>
    <div class="container">
        <table class="table">
            <thead class="thead">
            <tr>
                <th>Id</th>
                <th>Catégorie</th>
                <th>Titre</th>
                <th>Description</th>
            </tr>
            </thead>
            <tbody>
            {% for article in tag.articles %}
                <tr>
                    <td>{{ article.id }}</td>
                    <td>{{ article.category.name }}</td>
                    <td>{{ article.title }}</td>
                    <td>{{ article.content }}</td>
                </tr>
            {% else %}
                <tr>
                    <td>Pas d'articles associés</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
        <div class="row my-2 justify-content-start">
            <a class="btn btn-danger" href="{{ path('tag_index') }}">Retour</a>
        </div>
    </div>

{% endblock %}